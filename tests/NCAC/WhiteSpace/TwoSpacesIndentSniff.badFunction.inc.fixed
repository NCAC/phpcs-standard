<?php

function test_foo($string): bool {
    if ($string === 'yoyo') { // error should be 2 spaces
        return false; // error /should be 4 spaces
    } // error should be 2 spaces
    return true; // error should be 2 spaces
}
